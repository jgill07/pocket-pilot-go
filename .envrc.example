# Copy to .envrc and run `direnv allow`. The real .envrc is gitignored.
# Matches the credentials in docker-compose.yml (local dev only).
export DATABASE_URL="postgres://pocket_pilot:pocket_pilot@localhost:5432/pocket_pilot?sslmode=disable"
